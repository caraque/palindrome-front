import reducer from './reducer';

import * as appActions from './action';
import * as appSelectors from './selector';
import * as appTypes from './type';

export {appActions, appSelectors, appTypes, reducer};

export default reducer;
